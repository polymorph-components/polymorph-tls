[package]
name = "tls-virt-demo"
description = "Demo app for the tls-virt virtualizer: plain TCP through a suffixed hostname, transparently tunneled over TLS"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
wasip3 = { workspace = true }
