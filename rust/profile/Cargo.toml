[package]
name = "lann-tls-profile"
description = "The wasm-safe TLS 1.3 algorithm profile: policy as data, plus identity types that keep class-D private keys out of the guest"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
rustls = { workspace = true }
rustls-pki-types = { workspace = true }
ed25519-dalek = { workspace = true }
