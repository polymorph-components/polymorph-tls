[package]
name = "tls-virt-guest"
description = "wasi:sockets virtualizer component: transparent TLS tunneling for suffix-opted hostnames via the composed polymorph:tls client"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
tls-virt-common = { workspace = true }
wit-bindgen = { workspace = true }
futures = { workspace = true }
