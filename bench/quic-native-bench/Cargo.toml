[package]
name = "quic-native-bench"
description = "Native mirror of the quic-loopback bench mode: the same noq-proto + profile stack over std::net UDP, isolating the wasm-vs-native delta"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
polymorph-tls-profile = { workspace = true }
polymorph-tls-quic = { workspace = true }
noq-proto = { workspace = true }
bytes = "1"
rustls = { workspace = true }
rustls-pki-types = { workspace = true }
