[package]
name = "tls-virt-demo-p2"
description = "WASI 0.2 demo app for the tls-virt-wasmtime delivery: plain std::net TCP through a suffixed hostname, transparently tunneled over TLS"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
