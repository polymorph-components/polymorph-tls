[package]
name = "webcrypto-signer"
description = "The polymorph:tls signer shim over a polymorph:webcrypto provider: TLS CertificateVerify signatures from a webcrypto-held Ed25519 key"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { workspace = true }
futures = { workspace = true }
