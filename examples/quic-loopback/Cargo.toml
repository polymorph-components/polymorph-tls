[package]
name = "quic-loopback"
description = "Loopback smoke test: two QUIC endpoints over wasi:sockets UDP under a WASI runtime"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
polymorph-tls-quinn = { workspace = true }
polymorph-tls-profile = { workspace = true }
quinn-proto = { workspace = true }
rustls = { workspace = true }
rustls-pki-types = { workspace = true }

[target.'cfg(all(target_family = "wasm", target_os = "wasi"))'.dependencies]
bytes = { workspace = true }
wasi = { workspace = true }
