[workspace]
members = ["rust/profile", "rust/tls", "rust/quinn", "rust/quinn-wasi", "examples/quic-loopback"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[workspace.dependencies]
lann-tls-profile = { path = "rust/profile" }

rustls = { version = "0.23", default-features = false, features = ["std"] }
rustls-pki-types = "1"
rustls-rustcrypto = { git = "https://github.com/RustCrypto/rustls-rustcrypto", default-features = false, features = ["std", "zeroize"] }
quinn-proto = { version = "0.11", default-features = false }

ed25519-dalek = { version = "3", default-features = false, features = ["alloc", "pkcs8", "zeroize"] }

lann-tls = { path = "rust/tls" }
lann-tls-quinn = { path = "rust/quinn" }
lann-quinn-wasi = { path = "rust/quinn-wasi" }

wasi = "0.14"

aead = { version = "0.6", default-features = false, features = ["alloc"] }
aes = "0.9"
aes-gcm = { version = "0.11", default-features = false, features = ["aes", "alloc"] }
chacha20 = "0.10"
chacha20poly1305 = { version = "0.11", default-features = false, features = ["alloc"] }
hkdf = "0.13"
hmac = "0.13"
sha2 = { version = "0.11", default-features = false }
bytes = "1"
