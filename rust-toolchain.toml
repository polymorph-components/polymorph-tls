[toolchain]
# Pin the Rust toolchain so contributors and automation build with the exact
# same compiler, and so the wasm target the components need is always
# available. Bump this deliberately rather than tracking floating `stable`.
channel = "1.96.0"
components = ["rustfmt", "clippy"]
targets = ["wasm32-wasip2"]
